<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cook_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cook_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        >
        <activity android:name=".MemoActivity"></activity>
        <activity android:name=".WasabiActivity"></activity>
        <activity android:name=".TunaActivity"></activity>
        <activity android:name=".EggActivity"></activity>
        <activity android:name=".PorkActivity"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>