<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cook_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cook_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LastActivity"></activity>
        <activity android:name=".MemoActivity" />
        <activity android:name=".WasabiActivity" />
        <activity android:name=".TunaActivity" />
        <activity android:name=".EggActivity" />
        <activity android:name=".PorkActivity" />
        <activity android:name=".ReviewActivity" />
        <activity android:name=".CreateQR" />
        <activity android:name=".QRActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>